import React from 'react';
import ModalSearchBar from './ModalSearchBar';

const index = ({ closeMobileNav }) => {
  return (
    <>
      <ModalSearchBar closeMobileNav={closeMobileNav} />
    </>
  );
};

export default index;
